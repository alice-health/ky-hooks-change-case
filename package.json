{
  "name": "@alice-health/ky-hooks-change-case",
  "version": "0.0.1",
  "description": "Allow modifications cases on requests and responses of the objects during the ky request",
  "main": "dist/index.js",
  "license": "MIT",
  "publishConfig": { "registry": "https://npm.pkg.github.com/" },
  "dependencies": {
    "lodash": "^4.17.20"
  },
  "scripts": {
    "build": "rollup index.js --format=umd --name=ky-hooks-change-case --dir=dist",
    "test": "npm run build && ava"
  },
  "devDependencies": {
    "ava": "^3.13.0",
    "create-test-server": "^3.0.1",
    "form-data": "^3.0.0",
    "ky": "^0.24.0",
    "ky-universal": "^0.8.2",
    "rollup": "^2.28.2"
  },
  "ava": {
    "require": [
      "./test/_require.js"
    ]
  }
}
